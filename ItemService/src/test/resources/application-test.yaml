spring:
  sql:
    init:
      mode: always
  cloud:
    aws:
      s3:
        enabled: false
      region:
        static: eu-north-1
      credentials:
        access-key: test
        secret-key: test
logging:
  level:
    org:
      springframework:
        r2dbc: DEBUG
    io:
      r2dbc: DEBUG