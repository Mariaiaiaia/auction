spring:
  sql:
    init:
      mode: always
  cloud:
    aws:
      s3:
        endpoint: http://localstack:4566
      region:
        static: eu-north-1
      credentials:
        access-key: test
        secret-key: test
logging:
  level:
    org:
      springframework:
        r2dbc: DEBUG
    io:
      r2dbc: DEBUG